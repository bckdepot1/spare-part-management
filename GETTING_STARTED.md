# คู่มือหลัง Deploy — เริ่มใช้งานจริง

ระบบ deploy เสร็จสมบูรณ์แล้วที่ **https://spare-part-management.bckdepot1.workers.dev**
เชื่อมกับฐานข้อมูล Supabase จริง ทุกคนที่เข้าเว็บนี้ (จากอุปกรณ์ไหนก็ได้) จะเห็นข้อมูลชุดเดียวกัน

## 1. ทดสอบล็อกอินให้ครบทั้ง 3 สิทธิ์

เข้า https://spare-part-management.bckdepot1.workers.dev แล้วล็อกอินทีละบัญชี ตรวจว่า:

| บัญชี | ควรทำได้ |
| --- | --- |
| `admin` | เห็นเมนู "จัดการผู้ใช้งาน", แก้ไข Min/Max ในคลังได้, อนุมัติรายการได้ |
| `supervisor` | อนุมัติรายการได้, แก้ไข Min/Max ได้, **ไม่เห็น** "จัดการผู้ใช้งาน" |
| `operator` | รับเข้า/เบิกออกได้ (ระบบจะพักไว้ "รออนุมัติ" ไม่ตัด stock ทันที) |

ลองรับอะไหล่เข้า 1 รายการด้วยบัญชี `admin` แล้วดูว่าตัวเลขคงเหลือในหน้า "รายการอุปกรณ์" เปลี่ยนทันทีไหม — ถ้าใช่ แปลว่าเชื่อมฐานข้อมูลสมบูรณ์

## 2. เพิ่มบัญชีพนักงานจริง

มี 2 วิธี:

**วิธีที่ 1 — ให้พนักงานสมัครเอง (แนะนำ)**
พนักงานเข้าเว็บ → กด "สร้างบัญชีใช้งาน" → กรอกข้อมูล → ระบบจะสร้างบัญชีสถานะ "รออนุมัติ" อัตโนมัติ
จากนั้น `admin` หรือ `supervisor` เข้าเมนู **จัดการผู้ใช้งาน** → กด "อนุมัติ" ที่บัญชีนั้น เป็นอันเสร็จ (เริ่มต้นเป็นสิทธิ์ Operator เสมอ ถ้าต้องการเลื่อนสิทธิ์ให้ Admin ไปปรับที่ dropdown สิทธิ์ในหน้าเดียวกัน)

**วิธีที่ 2 — Admin สร้างให้เอง**
ไปที่ Supabase Dashboard → Authentication → Users → Add user เหมือนตอนสร้างบัญชีเดโม (ดู `SUPABASE_SETUP.md` ขั้นตอนที่ 4) แล้วรัน SQL ปรับ role/status เอง

> ลบบัญชีเดโม 3 บัญชี (`admin`/`supervisor`/`operator`) ออกได้ถ้าไม่ต้องการให้มีบัญชีทดสอบปนอยู่ในระบบจริง — ลบได้จากหน้า "จัดการผู้ใช้งาน" (ยกเว้นบัญชีที่ใช้งานอยู่ตอนนั้น) หรือลบใน Supabase Dashboard → Authentication → Users

## 3. อัปเดตโค้ดในอนาคต

ทุกครั้งที่ต้องแก้ไขเว็บ (เช่น ขอเพิ่มฟีเจอร์ใหม่กับผม) — พอโค้ดพร้อม แค่:

```bash
git add -A
git commit -m "อธิบายสิ่งที่แก้"
git push
```

Cloudflare จะ build และ deploy เวอร์ชันใหม่ให้อัตโนมัติภายใน 1-2 นาที ไม่ต้องทำอะไรเพิ่มที่ฝั่ง Cloudflare หรือ Supabase เลย (ยกเว้นถ้ามีการแก้โครงสร้างฐานข้อมูล จะมี SQL ไฟล์ใหม่ให้รันเพิ่มใน Supabase SQL Editor)

## 4. (ถ้าต้องการ) ใช้โดเมนของบริษัทเอง

แทนที่จะใช้ `spare-part-management.bckdepot1.workers.dev` สามารถผูกโดเมนของบริษัทได้ (เช่น `spm.บริษัทคุณ.co.th`) — ไปที่ Cloudflare Dashboard → เลือกโปรเจกต์นี้ → แท็บ **Domains** → **Add domain** — ถ้าต้องการให้ช่วยตั้งค่า DNS แจ้งมาได้เลย

## 5. สำรองข้อมูล (แนะนำให้ตั้งเป็นนิสัย)

Supabase มีระบบ backup อัตโนมัติในตัว (ดูที่ Dashboard → Database → Backups) แต่ในแผนฟรีจะเก็บย้อนหลังได้จำกัดวัน ถ้าข้อมูลสำคัญมาก แนะนำ export ตาราง `stock` และ `transactions` เป็นไฟล์เก็บไว้เป็นระยะ (Table Editor → เลือกตาราง → Export)

## สรุปเอกสารทั้งหมดในโปรเจกต์

| ไฟล์ | ใช้ตอนไหน |
| --- | --- |
| `README.md` | ภาพรวมระบบ, ตารางสิทธิ์การใช้งาน, โครงสร้างไฟล์ |
| `SUPABASE_SETUP.md` | ตั้งค่าฐานข้อมูลครั้งแรก (ทำไปแล้ว) |
| `DEPLOY.md` | ตั้งค่า deploy ครั้งแรก (ทำไปแล้ว) |
| `GETTING_STARTED.md` | ไฟล์นี้ — สิ่งที่ทำหลัง deploy เสร็จ |

มีคำถามหรือขอปรับแก้ฟีเจอร์อะไรเพิ่ม แจ้งได้เลยครับ
